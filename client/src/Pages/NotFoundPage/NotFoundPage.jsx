import React from 'react';
import NotFound from '../../components/NotFoundPageComponents/NotFound';

const NotFoundPage = () => {
  return (
    <div>
      <NotFound />
    </div>
  );
};

export default NotFoundPage;
